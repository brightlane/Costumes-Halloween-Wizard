# Vulture Engine — build dependencies
# All core modules (os, datetime, urllib, collections) are Python stdlib — no install needed.
# This file intentionally minimal to prevent pip install failures in GitHub Actions.

# No third-party dependencies required.
# Add here only if you introduce new imports to build.py.
